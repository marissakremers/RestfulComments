<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>

    <%- include('../partials/styling') %>
    <link rel="stylesheet" type="text/css" href="/css/show.css">
</head>

<body>
    <!-- Form rendered using: SHOW - details about one particular comment (GET using id) -->
    <h1>Show Comment</h1>

    <h2> <%= comment.comment %> - <%=comment.username %></h2>
    
    <span>
        <!-- INDEX - display all comments (GET) -->
        <a href="/comments">Back to index</a>

        <!-- EDIT - renders a form to edit a comment (GET using id) -->
        <a href="/comments/<%= comment.id%>/edit">Edit Comment</a>

        <!-- DELETE- removes a single comment (DELETE using id) -->
        <form method="POST" action="/comments/<%= comment.id %>?_method=DELETE">
            <button id="delete">Delete</button>
        </form>
    </span>
</body>

</html>